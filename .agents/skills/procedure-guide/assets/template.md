# <手順書のタイトル>

> この手順書でできること（1〜2行）。所要時間や前提（必要なアカウント・OS）があれば。

## 前提

- 必要なもの（アカウント・ツール・権限）を箇条書きで。
- 用語は初出に「（＝平たい説明）」を添える。

## 手順

### ステップ1：<このステップで何をするか>

何のためにやるか（1行）。

```bash
echo "シェルで実行する1単位。これで1つのコピーボタンになる"
```

> ⚠ ここでよくある失敗を1〜2行で。

> ✅ 確認：うまくいったときに見える状態（例：`dist/` ができている）。

### ステップ2：<GUI操作の例>

ブラウザやアプリの操作は番号手順で書く（コピーボタンは付かない）。

1. <サイト名> を開く。
2. メニューの「○○」を選ぶ。
3. 「作成」を押す。

> ⚠ 種別を間違えやすい。「△△」ではなく「□□」を選ぶ。

> ✅ 確認：一覧に新しい項目が増えている。

### ステップ3：<複数コマンドを1単位で実行する例>

途中で止まらず一気に実行してよいものは && で連結して1ボタンにできる。

```bash
cd myapp && npm install && npm run build
```

> ⚠ ブラウザ起動や出力確認を挟むコマンドは連結せず、別ブロックに分ける。

## 困ったとき

- よくあるエラーと対処を2〜3個。
