# レビューパック — Web支出内訳グラフのレイアウト修正

## 区分

- [x] 軽量（CSS競合による既存画面のバグ修正）
- [ ] フル

## 対象ドキュメント

- 要求: [requirements.md](./requirements.md)
- 設計: [design.md](./design.md)
- タスク: [tasklist.md](./tasklist.md)

## トレーサビリティ表

| 受け入れ条件                  | 設計要素                            | タスク   | 状態     |
| ----------------------------- | ----------------------------------- | -------- | -------- |
| AC-1 PCで棒グラフを横並び表示 | PC側の汎用display上書きを解消       | T-1, T-2 | 対応あり |
| AC-2 モバイルで縦リストを維持 | モバイル側の非表示指定を維持        | T-1, T-2 | 対応あり |
| AC-3 品質確認に合格           | lint・型チェック・対象E2Eと監査記録 | T-2, T-3 | 対応あり |

> 漏れ・孤立の有無: なし。

## 前提・未決事項

### 要ユーザー判断

- なし。修正方針と実施はユーザー承認済み。

### 設計上の前提

- 1023px以下をモバイル表示とする既存仕様は維持する。
- 集計ロジックや表示文言は変更しない。
- 実在のPII・機微データは扱わない。

## 影響範囲

- `docs/` への影響 / 更新案: なし。
- 既存コード・機能への影響: `.desktop-only` のPC表示方式と支出月次グラフ。
- マイグレーション・後方互換: いずれも不要。

## セルフレビュー結果

| 観点                       | 指摘                                                 | 対応                                |
| -------------------------- | ---------------------------------------------------- | ----------------------------------- |
| UX/UI                      | 棒グラフだけを直しモバイル表示を壊さないこと         | 両表示幅をE2Eで検証                 |
| アーキテクチャ             | `!important` は競合を隠す                            | 汎用クラスからPCのdisplay指定を除去 |
| QA                         | 既存テストは可視性だけでレイアウト方式を検出できない | 算出済み `display` の検証を追加     |
| セキュリティ・プライバシー | 実データは不要                                       | 合成seedを使う既存E2Eのみ実行       |

### 第二意見

- 軽量区分のため不要。

## 承認

- [x] 未決事項ゼロを確認
- [x] トレーサビリティ表に漏れ・孤立なし
- [x] 2026-07-25、ユーザーの「進めて」により要求・設計・タスクをまとめて承認
