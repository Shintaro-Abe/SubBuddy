# レビューパック — TestFlight スプリント ロードマップ

> このファイル1枚で `roadmap.md` を承認する。

## 区分

- [x] 軽量（計画のみ。`docs/` の基本設計・コードに触れない）
- [ ] フル

計画ドキュメントであり実装・スキーマ変更を含まないため軽量区分。ただし決定3・4（entitlement リスク受容と延期許容）はスケジュールの前提を固定するため、承認をもって以後の差し戻しはグリル再実施とする。

2026-07-07 更新：サーバー側整備は別ステアリング（`../20260707-testflight-backend-readiness/`）で完了済み。親ロードマップは、以後の主作業を iOS 実装・Render 実デプロイ・7日連続計測へ寄せる。

## 対象ドキュメント

- 計画：[roadmap.md](./roadmap.md)
- iOS 実装前決定：[ios-implementation-decisions.md](./ios-implementation-decisions.md)
- 完了済み子ステアリング：[`../20260707-testflight-backend-readiness/`](../20260707-testflight-backend-readiness/)

## 前提・未決事項

### 決定済み（グリルで解消済み）

- 到達点・期限・スコープ・フォールバック・両立方針・保守の扱い（決定簿1〜9）。

### 要ユーザー判断（状態更新）

- [x] v1 計測3点はスプリントに含める。後続ステアリングの優先判断に必要な最小イベントであり、機能追加ではなく観測点として扱う。
- [ ] 任意項目「事前推定1問」は W2 時点の余裕で判断する。これは承認ブロッカーではなく、余裕がなければ入れない。

### 設計上の前提（崩れると計画が変わるもの）

- 前提1：Family Controls entitlement は本体・Extension とも承認済み（2026-07-06確認）。以後は配布プロファイルと Archive に entitlement が入ることを `codesign` で確認する。
- 前提2：道場発表の本番日は4週間以内（〜8月初旬）で確定している。
- 前提3：7日連続計測は開発用 entitlement の自分の実機で成立する。
- 前提4：サーバー側 API・DB・手順書は 2026-07-07 時点で実装・検証済み。Render 実デプロイと Apple Developer の実設定は外部作業として残る。

## 影響範囲

- `wbs.yml`：本計画の行を追加する（planning repo 修正後に GitHub 同期）。
- `docs/`：サーバー側整備に伴う `functional-design.md` / `architecture.md` 差分は子ステアリングで反映済み。

## 承認

- [x] 承認ブロッカーとなる未決事項がないことを確認
- [x] 上記をもって roadmap.md を承認
