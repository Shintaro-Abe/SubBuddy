# タスクリスト — Screen Time計測ライフサイクルの自動化

## タスク

| # | タスク | 対応AC | 状態 | 完了条件 |
|---|---|---|---|---|
| T-1 | 永続的ドキュメントへ自動開始・変更・解除・削除の仕様を反映する | AC-3〜AC-8, AC-11 | 完了 | PRD、機能設計、用語集で状態と削除意味が一致する |
| T-2 | 計測状態を認可・契約・対応付けから導出する`MeasurementReconciler`を実装する | AC-1〜AC-3, AC-8, AC-9 | 進行中 | 起動・復帰・認可・契約更新後に監視状態が冪等に収束する |
| T-3 | 手動開始・停止UIを対象選択・変更・解除と状態表示へ置換する | AC-3, AC-4, AC-6, AC-8 | 進行中 | 利用者が内部の開始・停止を操作せず、変更と解除の影響を事前確認できる |
| T-4 | 対象変更・解除の中断可能な処理状態と同期保留をApp Groupへ実装する | AC-5, AC-6, AC-9 | 進行中 | 通信失敗・終了・再起動後も旧データと新データを混在させず再試行できる |
| T-5 | 所有権検証付き`DELETE /api/subscriptions/{id}/usage`とRepositoryのトランザクション削除を実装する | AC-5, AC-6, AC-9〜AC-11 | 完了 | 対象契約の集計と見直しだけを冪等に削除し、別利用者・別契約を拒否する |
| T-6 | 対象変更・解除後の見直し再計算と、削除済み利用量を表示しない更新処理を実装する | AC-5, AC-6, AC-11 | 完了 | 利用量なしの最新結果へ収束し、古い根拠が一覧・詳細に残らない |
| T-7 | iOS単体・結合テストを合成データで追加する | AC-1〜AC-9, AC-12 | 進行中 | 自動開始、認可後開始、変更、解除、契約削除、認可取消、失敗再開、別契約保持が合格する |
| T-8 | Web API・Repositoryのテナント境界と冪等性テストを合成データで追加する | AC-5, AC-6, AC-9〜AC-12 | 完了 | 所有者成功、他利用者拒否、存在しない契約、再実行、途中失敗を検証できる |
| T-9 | テスト台帳を更新し、lint・型チェック・build・自動テストを実施する | AC-1〜AC-12 | 進行中 | `.audit/test-status.md`に開始前・終了後を記録し、対象検証が合格する |
| T-10 | 合成契約で実機回帰を行い、開始・変更・解除・認可取消・再起動を確認する | AC-1〜AC-9, AC-11, AC-12 | 未着手 | 端末とクラウドの対象限定削除、状態収束、非計測経路を実機で確認できる |
| T-11 | CodeRabbitレビュー指摘を反映し、認可復帰・選択比較・APIクライアント再利用・障害ログを回帰確認する | AC-2, AC-8, AC-9, AC-12 | 進行中 | 表示中画面が認可変更へ追従し、選択比較と削除通信が安定し、500応答の原因を安全に追跡できる |

状態: 未着手 / 進行中 / 完了 / 保留

## 実装中の逸脱ログ

- T-5/T-6: DBスキーマは変えず、所有権確認と既存の利用量・見直しテーブル削除をトランザクション化した。削除後は既存の全件再計算を使い、対象契約を利用量なしへ収束させる。
- T-2〜T-4/T-7: Linux環境にはXcode・Apple Frameworkがないため、iOSコードと合成テストは追加済みだが、Simulator buildとiOSテストはMac実行待ち。実機確認前に完了扱いしない。
- T-5/T-6/T-8/T-9: Web全33ファイル188件、型チェック、変更Webファイルのlint、本番buildが合格した。全体lintは作業環境のファイル読込エラーで停止したため、変更ファイルの個別lintを合格証跡とし、未確認範囲を台帳へ残した。
- T-11: CodeRabbitの完全レビューとPR #8の再レビューで得た指摘を再検証し、有効な7件を修正した。Web全33ファイル188件、型チェック、変更Webファイルのlint、本番build、差分形式が再度合格。認可取消・再許可は`MeasurementSession.authorizationDidChange`の停止・再開を確認する合成テストへ置換し、注入したProduct APIから実通信へ逸脱しないテストも追加した。表示中画面の実機遷移とiOSテスト実行はMac待ちのため進行中を維持する。Docstring網羅率の既定警告は機能修正と分離する。
- 設計の根幹となる「手動開始・停止を持たない」「対象変更・解除で旧計測データを削除する」を変える場合は再承認を得る。

## 完了チェック

- [ ] 全タスク完了
- [ ] 全受け入れ条件を満たす（トレーサビリティ表の各行を検証済み）
- [ ] `.audit/test-status.md`を各独立試験の実行前後に更新
- [ ] lint・型チェック・build・自動テスト実施
- [ ] 合成データによる実機確認実施
- [ ] 必要な`docs/`更新を反映
- [ ] 実データ・PII・資格情報をコード、テスト、証跡へ含めていない
