# タスクリスト — iOSセッション更新競合の修正

## タスク

| #   | タスク                                                  | 対応AC           | 状態 | 完了条件                                           |
| --- | ------------------------------------------------------- | ---------------- | ---- | -------------------------------------------------- |
| T-1 | 接続先別の共有 `APIClient` を実装する                   | AC-1, AC-2       | 完了 | 同じURLで同じインスタンスを取得できる              |
| T-2 | iOS本番コードの認証済み通信を共有クライアントへ統一する | AC-1, AC-2       | 完了 | 対象箇所に独立した直接生成が残らない               |
| T-3 | 並行更新と構成境界の回帰テストを追加する                | AC-2, AC-3, AC-4 | 完了 | 合成データのテストを追加し、静的境界検査が合格する |
| T-4 | lint相当・静的検証・iOSテストを実行し監査台帳を更新する | AC-3, AC-4       | 完了 | 実行可能な検証が合格し、未確認範囲が記録される     |

状態: 未着手 / 進行中 / 完了 / 保留

## 実装中の逸脱ログ

- なし。

## 検証結果

- 同じURLの逐次・並行取得で同じ `APIClient` を返し、異なるURLは分離する合成XCTestを3件追加。
- iOS本番ソースの `APIClient(baseURL:)` は共有プール内部の1件だけであることを静的確認。
- 既存の `refreshTask` 共有経路が維持されていることを静的確認。
- Bundle ID `com.subbuddy.app`、本体表示名 `MUDASK`、`SUBBUDDY_API_BASE_URL` が不変であることを確認。
- `git diff --check` とステアリング文書のPrettier検査が合格。
- Linux環境にSwift・Xcodeがないため、Swift build、XCTest実行、Simulator、実機Appleサインインは未確認。

## 完了チェック

- [x] 全タスク完了
- [x] 全受け入れ条件を満たす
- [x] lint相当・静的検証を実施し、Macが必要なiOSテストを未確認範囲として記録
- [x] `docs/` 更新不要を確認
